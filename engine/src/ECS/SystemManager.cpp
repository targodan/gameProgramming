#include "SystemManager.h"

namespace engine {
    namespace ECS {
        SystemManager::SystemManager() {
        }

        SystemManager::SystemManager(const SystemManager& orig) {
        }

        SystemManager::~SystemManager() {
        }
    }
}

