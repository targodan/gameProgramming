#ifndef SYSTEMTYPE_H
#define SYSTEMTYPE_H

namespace engine {
    namespace ECS {
        enum class SystemType {
            UPDATE = 0,
            RENDER = 1
        };
    }
}

#endif /* SYSTEMTYPE_H */

