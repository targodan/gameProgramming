#include "Explosion.h"

namespace engine {
    namespace physics {
        Matrix<float, Dynamic, 1> Explosion::getForceOnVertices(const Matrix<float, Dynamic, 1>& verticies) const {
            // TODO: implement
        }
    }
}
